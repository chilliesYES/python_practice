>>> class C:
...     x = []#列表是单独存储的，
...     def add_x(self, x):
...         self.x.append(x)#这里的self.x可以理解为对类属性x的引用，看似是修改了某对象的私有属性实际上由于列表的存储导致实际上修改的是类属性
...
>>> c = C()
>>> d = C()
>>> c.add_x(250)
>>> d.add_x(520)
>>> c.x
>>> # 请问这里会打印什么内容？
